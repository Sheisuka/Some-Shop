[tool.flake8]
exclude = ['venv', 'migrations']
inline-quotes = '"'
import-order-style = 'google'
application-import-names = ['about', 'catalog', 'homepage', 'core']
count = true

[tool.black]
line-length = 79
exclude = 'venv'
